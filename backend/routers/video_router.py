def _get_video_info(video_id):
    # Assuming this function fetches video info without duplicates
    video_info = fetch_video_info(video_id)
    return video_info

def _generate_thumbnail(video_id):
    # Assuming this generates a thumbnail correctly
    thumbnail = create_thumbnail(video_id)
    return thumbnail
