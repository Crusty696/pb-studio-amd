# Inert path-dataflow artifact

This directory contains documentation only. It does not execute the vulnerable
CLI and does not create, modify, or truncate files.

Inspect the artifact from this directory:

```sh
cat crafted-labels-and-dataflow.txt
```

Do not pass these labels to the vulnerable program on a real checkout.
Execution and regression testing are deferred to T332.
